(* open Type

   type t = activity * var_inc * var_decay
   and activity = float Vector.t
   and var_inc = float
   and var_decay = float

   val update : t -> int -> unit
   val update_all : t -> unit
   val undo : t -> unit
   val select : t -> int *)
