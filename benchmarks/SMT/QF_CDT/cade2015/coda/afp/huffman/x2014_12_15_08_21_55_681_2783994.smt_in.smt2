;(set-option :produce-unsat-cores true )
(set-logic ALL_SUPPORTED )
(declare-sort A$ 0 )
(declare-sort Nat$ 0 )
(declare-sort A_set$ 0 )
(declare-sort A_nat_fun$ 0 )
(declare-sort A_tree$ 0)
(declare-fun select$ (A_tree$)Nat$)
(declare-fun selecta$ (A_tree$)A$)
(declare-fun leaf$ (Nat$ A$ )A_tree$)
(declare-fun selectb$ (A_tree$)Nat$)
(declare-fun selectc$ (A_tree$)A_tree$)
(declare-fun selectd$ (A_tree$)A_tree$)
(declare-fun innerNode$ (Nat$ A_tree$ A_tree$ )A_tree$)
(declare-fun a$ ()A$ )
(declare-fun b$ ()A$ )
(declare-fun c$ ()A$ )
(declare-fun t$ ()A_tree$ )
(declare-fun u$ ()A_tree$ )
(declare-fun w_a$ ()Nat$ )
(declare-fun w_b$ ()Nat$ )
(declare-fun cost$ (A_tree$ )Nat$ )
(declare-fun freq$ (A_tree$ )A_nat_fun$ )
(declare-fun plus$ (Nat$ Nat$ )Nat$ )
(declare-fun zero$ ()Nat$ )
(declare-fun height$ (A_tree$ )Nat$ )
(declare-fun member$ (A$ A_set$ )Bool )
(declare-fun minima$ (A_tree$ A$ A$ )Bool )
(declare-fun fun_app$ (A_nat_fun$ A$ )Nat$ )
(declare-fun less_eq$ (Nat$ Nat$ )Bool )
(declare-fun sibling$ (A_tree$ A$ )A$ )
(declare-fun alphabet$ (A_tree$ )A_set$ )
(declare-fun splitLeaf$ (A_tree$ Nat$ A$ Nat$ A$ )A_tree$ )
(declare-fun consistent$ (A_tree$ )Bool )
(declare-fun swapFourSyms$ (A_tree$ A$ A$ A$ A$ )A_tree$ )
(assert (! (not (minima$ u$ a$ b$ )):named a0 ))
(assert (! (forall ((?v0 A$ ))(=> (member$ ?v0 (alphabet$ t$ ))(and (less_eq$ w_a$ (fun_app$ (freq$ t$ )?v0 ))(less_eq$ w_b$ (fun_app$ (freq$ t$ )?v0 ))))):named a1 ))
(assert (! (member$ a$ (alphabet$ t$ )):named a2 ))
(assert (! (not (member$ b$ (alphabet$ t$ ))):named a3 ))
(assert (! (consistent$ u$ ):named a4 ))
(assert (! (= (fun_app$ (freq$ t$ )a$ )(plus$ w_a$ w_b$ )):named a5 ))
(assert (! (not (= a$ b$ )):named a6 ))
(assert (! (= (freq$ (splitLeaf$ t$ w_a$ a$ w_b$ b$ ))(freq$ u$ )):named a7 ))
(assert (! (= (alphabet$ (splitLeaf$ t$ w_a$ a$ w_b$ b$ ))(alphabet$ u$ )):named a8 ))
(assert (! (member$ a$ (alphabet$ u$ )):named a9 ))
(assert (! (member$ b$ (alphabet$ u$ )):named a10 ))
(assert (! (member$ c$ (alphabet$ u$ )):named a11 ))
(assert (! (= (cost$ (splitLeaf$ t$ w_a$ a$ w_b$ b$ ))(plus$ (plus$ (cost$ t$ )w_a$ )w_b$ )):named a12 ))
(assert (! (forall ((?v0 A_tree$ )(?v1 A$ )(?v2 A$ ))(= (minima$ ?v0 ?v1 ?v2 )(and (member$ ?v1 (alphabet$ ?v0 ))(and (member$ ?v2 (alphabet$ ?v0 ))(and (not (= ?v1 ?v2 ))(and (less_eq$ (fun_app$ (freq$ ?v0 )?v1 )(fun_app$ (freq$ ?v0 )?v2 ))(forall ((?v3 A$ ))(=> (and (member$ ?v3 (alphabet$ ?v0 ))(and (not (= ?v3 ?v1 ))(not (= ?v3 ?v2 ))))(and (less_eq$ (fun_app$ (freq$ ?v0 )?v1 )(fun_app$ (freq$ ?v0 )?v3 ))(less_eq$ (fun_app$ (freq$ ?v0 )?v2 )(fun_app$ (freq$ ?v0 )?v3 ))))))))))):named a13 ))
(assert (! (not (= (height$ (splitLeaf$ t$ w_a$ a$ w_b$ b$ ))zero$ )):named a14 ))
(assert (! (= (sibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ )b$ ):named a15 ))
(assert (! (consistent$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))):named a16 ))
(assert (! (= (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))(freq$ u$ )):named a17 ))
(check-sat )
;(get-unsat-core )
