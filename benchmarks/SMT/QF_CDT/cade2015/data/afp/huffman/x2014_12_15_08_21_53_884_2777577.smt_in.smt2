;(set-option :produce-unsat-cores true )
(set-logic ALL_SUPPORTED )
(declare-sort A$ 0 )
(declare-sort Nat$ 0 )
(declare-sort A_set$ 0 )
(declare-sort A_nat_fun$ 0 )
(declare-datatypes ()((A_tree$ (leaf$ (select$ Nat$ )(selecta$ A$ ))(innerNode$ (selectb$ Nat$ )(selectc$ A_tree$ )(selectd$ A_tree$ )))))
(declare-fun a$ ()A$ )
(declare-fun b$ ()A$ )
(declare-fun c$ ()A$ )
(declare-fun t$ ()A_tree$ )
(declare-fun u$ ()A_tree$ )
(declare-fun w_a$ ()Nat$ )
(declare-fun w_b$ ()Nat$ )
(declare-fun cost$ (A_tree$ )Nat$ )
(declare-fun freq$ (A_tree$ )A_nat_fun$ )
(declare-fun plus$ (Nat$ Nat$ )Nat$ )
(declare-fun member$ (A$ A_set$ )Bool )
(declare-fun fun_app$ (A_nat_fun$ A$ )Nat$ )
(declare-fun less_eq$ (Nat$ Nat$ )Bool )
(declare-fun sibling$ (A_tree$ A$ )A$ )
(declare-fun alphabet$ (A_tree$ )A_set$ )
(declare-fun splitLeaf$ (A_tree$ Nat$ A$ Nat$ A$ )A_tree$ )
(declare-fun consistent$ (A_tree$ )Bool )
(declare-fun mergeSibling$ (A_tree$ A$ )A_tree$ )
(declare-fun swapFourSyms$ (A_tree$ A$ A$ A$ A$ )A_tree$ )
(assert (! (not (= (plus$ (plus$ (cost$ (mergeSibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ ))w_a$ )w_b$ )(cost$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))))):named a0 ))
(assert (! (= (sibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ )b$ ):named a1 ))
(assert (! (not (= a$ b$ )):named a2 ))
(assert (! (not (= (sibling$ u$ c$ )c$ )):named a3 ))
(assert (! (= w_a$ (fun_app$ (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))a$ )):named a4 ))
(assert (! (= w_b$ (fun_app$ (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))b$ )):named a5 ))
(assert (! (= (plus$ (plus$ (cost$ (mergeSibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ ))(fun_app$ (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))a$ ))(fun_app$ (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))(sibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ )))(cost$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))):named a6 ))
(assert (! (consistent$ u$ ):named a7 ))
(assert (! (less_eq$ w_a$ w_b$ ):named a8 ))
(assert (! (= (freq$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))(freq$ u$ )):named a9 ))
(assert (! (member$ c$ (alphabet$ u$ )):named a10 ))
(assert (! (= (fun_app$ (freq$ t$ )a$ )(plus$ w_a$ w_b$ )):named a11 ))
(assert (! (consistent$ (mergeSibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ )):named a12 ))
(assert (! (consistent$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))):named a13 ))
(assert (! (= (alphabet$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ )))(alphabet$ u$ )):named a14 ))
(assert (! (= (freq$ (mergeSibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ ))(freq$ t$ )):named a15 ))
(assert (! (less_eq$ (plus$ (plus$ (cost$ t$ )w_a$ )w_b$ )(plus$ (plus$ (cost$ (mergeSibling$ (swapFourSyms$ u$ a$ b$ c$ (sibling$ u$ c$ ))a$ ))w_a$ )w_b$ )):named a16 ))
(assert (! (= (freq$ (splitLeaf$ t$ w_a$ a$ w_b$ b$ ))(freq$ u$ )):named a17 ))
(check-sat )
;(get-unsat-core )
